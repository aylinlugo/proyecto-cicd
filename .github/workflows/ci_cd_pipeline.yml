name: CI/CD Pipeline con Docker y Railway

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest
    env:
      NODE_ENV: production

    defaults:
      run:
        working-directory: ./app  # <- todos los comandos se ejecutan dentro de /app

    steps:
      # 1. Descargar el código
      - name: Checkout del repositorio
        uses: actions/checkout@v3

      # 2. Configurar Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"

      # 3. Instalar dependencias (incluye devDependencies como jest)
      - name: Instalar dependencias
        run: npm install

      # 4. Ejecutar pruebas unitarias con Jest
      - name: Ejecutar pruebas
        run: npx jest --runInBand

      # 5. Generar versión semántica automática
      - name: Generar versión semántica
        run: |
          tag=$(git tag --sort=-v:refname | head -n 1)
          if [ -z "$tag" ]; then
            tag="v1.0.0"
          else
            IFS='.' read -r major minor patch <<< "${tag#v}"
            patch=$((patch+1))
            tag="v$major.$minor.$patch"
          fi
          echo "Nueva versión: $tag"
          echo "next_tag=$tag" >> $GITHUB_ENV
        shell: bash

      # 6. Login a Docker Hub
      - name: Login a Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASS }}

      # 7. Build y push de Docker
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_USER }}/proyecto-cicd:latest

      # 8. Deploy a Railway
      - name: Deploy to Railway
        run: |
          echo "Deploy usando Railway: imagen ya subida a Docker Hub"
